<com.excellence.bouncebackview.PullToRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/refresh_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include layout="@layout/refresh_head" />
    <!-- 支持所有实现Pullable接口的View -->

    <com.excellence.bouncebackview.pullableview.PullableGridView
        android:id="@+id/content_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/light_blue"
        android:columnWidth="90dp"
        android:gravity="center"
        android:horizontalSpacing="10dp"
        android:numColumns="1"
        android:stretchMode="columnWidth"
        android:fadeScrollbars="false"
        android:scrollbarSize="5dp"
        android:overScrollMode="never"
        android:verticalSpacing="15dp" />

    <include layout="@layout/load_more" />

</com.excellence.bouncebackview.PullToRefreshLayout>